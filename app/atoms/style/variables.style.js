import { PixelRatio } from 'react-native';

// utility functions
import { size, percentage } from '../../../utilities/style.utilities';

/*
 * Style constants
 */

// spacing
export const MARGIN = size(20);
export const COLUMN_WIDTH = size(20);
export const GUTTER_WIDTH = size(16);

// elements
export const WRAPPER_HEIGHT = percentage(96);
export const WRAPPER_WIDTH = percentage(96);

